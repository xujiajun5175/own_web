# Cargo.toml (Rust)
[package]
name = "own-rust"
version = "0.1.0"
edition = "2021"
description = "Hello Rust project"

[lib]
crate-type = ["cdylib"]


[dependencies]
web-sys = { version = "0.3", features = ["console"] }
wasm-bindgen = "0.2.104"

[dev-dependencies]
